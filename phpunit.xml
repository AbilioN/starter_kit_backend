<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory suffix="Test.php">./tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory suffix=".php">./app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="LANDLORD_DB_CONNECTION" value="sqlite"/>
        <env name="LANDLORD_DB_DATABASE" value="storage/app/sqlite/testing-landlord.sqlite"/>
        <env name="TENANT_DB_CONNECTION" value="sqlite"/>
        <env name="TENANT_DB_DATABASE" value="storage/app/sqlite/testing-tenant.sqlite"/>
        <!-- Own Redis DB index, isolated from the dev keyspace (REDIS_DB=0).
             Tests push/pop raw, unprefixed keys like "openai_requests" (see
             ProcessOpenAIRequestAgentProfileTest) - since REDIS_PREFIX had to
             become empty for the real Python worker/openai-listener to see
             Laravel's writes (see .env's REDIS_PREFIX comment), those same
             raw keys are now also live in dev. Without a separate DB index
             here, a real dev worker can pop a test's queued payload before
             the test's own assertion does, flaking it. -->
        <env name="REDIS_DB" value="5"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
